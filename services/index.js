const {
  getAllBooks,
  getBookByISBN,
  getBooksByAuthor,
  getBooksByTitle,
  searchBooksByISBN,
  searchBooksByAuthor,
  searchBooksByTitle,
} = require("./books.service");

module.exports = {
  getAllBooks,
  getBookByISBN,
  getBooksByAuthor,
  getBooksByTitle,
  searchBooksByISBN,
  searchBooksByAuthor,
  searchBooksByTitle,
};
