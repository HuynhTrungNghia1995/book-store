const {
  getAllBooks,
  getBookByISBN,
  getBooksByAuthor,
  getBooksByTitle,
  searchBooksByISBN,
  searchBooksByAuthor,
  searchBooksByTitle,
  getBooksReviews,
} = require("./books.service");

module.exports = {
  getAllBooks,
  getBookByISBN,
  getBooksByAuthor,
  getBooksByTitle,
  searchBooksByISBN,
  searchBooksByAuthor,
  searchBooksByTitle,
  getBooksReviews,
};
